I"—<div class="fence-full">
    <div>
        <span class="fw-bold h3 text-uppercase mb-4">Tags</span>
    </div>

    {% for tag in site.tags %}
    <div id="{{ tag[0] | downcase }}" style="padding-top: 64px;border-bottom: 2px solid rgba(0,0,0,0.2); margin-bottom: 1em"><span class="h5 fw-bold text-capitalize">{{ tag[0] }}</span></div>

    {% assign pages_list = tag[1] %}
    {% for post in pages_list %}
    {% if post.title != null %}
    {% if group == null or group == post.group %}
    {% include main-loop-card.html %}
    {% endif %}
    {% endif %}
    {% endfor %}
    {% assign pages_list = nil %}
    {% assign group = nil %}

    {% endfor %}
</div>
:ET